// ignore_for_file: prefer_const_constructors

import 'package:flutter/material.dart';

class AppDecoration {
  static BoxShadow topBoxShadow = BoxShadow(
    color: Colors.white,
    // offset: const Offset(0, 0),
    // blurRadius: 2,
  );

  static BoxShadow bottomBoxShadow = BoxShadow(
    color: Colors.white,
    // offset: const Offset(0, -0),
    // blurRadius: 2,
  );

  static BoxShadow leftBoxShadow = BoxShadow(
    color: Colors.white,
    // offset: const Offset(-0, 0),
    // blurRadius: 2,
  );

  static BoxShadow rightBoxShadow = BoxShadow(
    color: Colors.white,
    // offset: const Offset(0, 0),
    // blurRadius: 2,
  );
}
